language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t alvasli/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true alvasli/docker-react npm run test
